/*#include <iostream>
using namespace std;

int main(){
	double avg;
	
	cout<<"Insert the average: ";
	cin>>avg;
	
	(avg>=75) ? cout<<"Very Good" : ((avg>=55) ? cout<<"Good" : ((avg>=40) ? cout<<"Pass" : cout<<"Fail") );
		
	return 0;
}

*/
